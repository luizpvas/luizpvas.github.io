<!DOCTYPE html>
<html lang="en">
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>luizpvas.github.io</title>
    <link rel="stylesheet" href="index.css" />
  </head>
  <body class="bg-slate-100">
    <main class="max-w-screen-md mx-auto my-4 space-y-4">
      <h1 class="text-3xl text-center tracking-tight">All posts</h1>

      <div class="flex justify-center items-center">
        <a href="https://github.com/luizpvas/" target="_blank" class="font-bold border rounded-full px-2 bg-white hover:border-blue-500">
          Github
        </a>
      </div>

      <div class="grid gap-2 mx-2 lg:mx-0">
        <% posts.each do |post| %>
          <a href="/<%= post.id %>.html" class="block border-2 bg-white rounded p-4 hover:border-blue-500">
            <div class="font-bold text-lg"><%= post.metadata.title %></div>

            <p><%= post.metadata.summary %></p>

            <div class="mt-1 text-sm text-slate-500">Published on <%= post.metadata.publication_date %></div>
          </a>
        <% end %>
      </div>
    </main>
  </body>
</html>
